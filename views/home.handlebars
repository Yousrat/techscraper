<div class="container">
    <div class="row">

     <ul class="nav nav-tabs">
                <li class="active"><a href="/"><span class="glyphicon glyphicon-home"></span> Home</a></li>
                <li><a href="/articles"><span class="glyphicon glyphicon-user"></span> Saved Articles</a></li>
                <li><a href="/scrape"><span class="glyphicon glyphicon-user"></span> Scrape New Articles</a></li>

    </ul>

        <div class="col-sm-8 col-sm-offset-2">
            <p class="cool-font text-center">{{title}}</p>
            <br>
        </div>
    </div>
    <div class="row">
           
{{#each items }}
                <div class="panel panel-default">
                    <div class="panel-heading">
                    <h4>{{this.title}}</h4>

                    {{!-- <form method="POST" action="/scrape">
                            <button type="submit" class="btn-lg btn-primary submit center-block" id="add-new-burger">Save</button>
                            </form> --}}
                    </div>
                    
                            <p>
                                 <a href="{{this.link}}">{{this.link}}</a>
                            </p>
                            
                </div>
        {{/each}}

    </div>
                        {{!-- <div class="col-md-8"> --}}

            {{!-- <div class="col-md-4"> --}}
                    

{{!--             </div>
 --}}
    </div>

</div>